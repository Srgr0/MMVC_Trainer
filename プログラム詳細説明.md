# MMVC_Trainer プログラム詳細説明書

**作成日**: 2025年6月4日  
**バージョン**: v1.3.2.12  
**目的**: このプログラムを後で再現するための詳細な技術仕様書

## 1. プロジェクト概要

### 1.1 プロジェクトの目的
MMVC_Trainerは、AIを使ったリアルタイムボイスチェンジャー「MMVC(RealTime-Many to Many Voice Conversion)」で使用するモデルを学習するためのPythonアプリケーションです。

### 1.2 技術スタック
- **フレームワーク**: PyTorch 1.13.1+cu116
- **音声処理**: torchaudio 0.13.1+cu116, librosa 0.8.0
- **モデルアーキテクチャ**: VITS (Variational Inference with adversarial learning for end-to-end Text-to-Speech)
- **学習環境**: Google Colaboratory推奨
- **音声フォーマット**: 24kHz 16bit モノラル WAV
- **テキスト処理**: 日本語音素（pyopenjtalk使用）

### 1.3 コンセプト
「簡単」「だれでも」「好きな声に」「リアルタイムで」ボイスチェンジを実現

## 2. ディレクトリ構造と各ファイルの役割

### 2.1 メインスクリプト
```
train.py              - 単一GPU学習用メインスクリプト
train_ms.py          - マルチスピーカー学習用メインスクリプト（推奨）
models.py            - SynthesizerTrn, MultiPeriodDiscriminatorモデル定義
data_utils.py        - データローダー実装
losses.py            - 損失関数（generator_loss, discriminator_loss, feature_loss, kl_loss）
```

### 2.2 前処理・後処理スクリプト
```
create_dataset_jtalk.py  - 日本語音素変換・ファイルリスト作成
onnx_export.py          - PyTorchモデル → ONNX変換
filelists/              - 学習・検証用ファイルリスト格納
configs/               - 設定ファイルテンプレート（baseconfig.json）
```

### 2.3 ニューラルネットワーク構成要素
```
modules.py           - ResidualCouplingBlock, WaveNet等の基本モジュール
attentions.py        - Multi-Head Attention実装
commons.py           - 共通ユーティリティ関数
transforms.py        - 正規化フロー変換
mel_processing.py    - メルスペクトログラム処理
```

### 2.4 設定・データ管理
```
utils.py             - ユーティリティ関数（ロード・セーブ等）
text/symbols.py      - 音素記号定義
text/cleaners.py     - テキスト正規化
monotonic_align/     - Cython実装のアライメント最適化
```

### 2.5 Jupyter Notebook ワークフロー
```
notebook/
  ├─ 00_Clone_Repo.ipynb         - リポジトリ初期設定
  ├─ 01_Create_Configfile.ipynb  - 設定ファイル生成
  ├─ 02_Train_MMVC.ipynb         - モデル学習実行
  ├─ 03_MMVC_Interface.ipynb     - 音声変換テスト
  └─ 04_Export_ONNX.ipynb        - ONNX形式エクスポート
```

## 3. 機械学習モデルアーキテクチャ

### 3.1 SynthesizerTrn（生成器）
- **ベース**: VITS (Variational Inference Text-to-Speech)
- **エンコーダー**: Posterior Encoder (enc_q) + Text Encoder (enc_p)
- **デコーダー**: HiFi-GAN Generator (dec)
- **フロー**: Normalizing Flow (flow)
- **話者埋め込み**: Speaker Embedding (emb_g)

### 3.2 MultiPeriodDiscriminator（識別器）
- **構成**: 複数の周期的パターンを学習する識別器
- **目的**: 生成音声の品質向上とリアリティ向上

### 3.3 損失関数
- **generator_loss**: 生成器の敵対的損失
- **discriminator_loss**: 識別器の敵対的損失
- **feature_loss**: 特徴マッチング損失
- **kl_loss**: KLダイバージェンス損失
- **l1_loss**: メルスペクトログラム復元損失

## 4. データ処理パイプライン

### 4.1 音声データ前処理
1. **音声読み込み**: 24kHz 16bit モノラル WAV
2. **スペクトログラム変換**: STFT → メルスペクトログラム
3. **正規化**: audio_norm = audio / max_wav_value
4. **キャッシュ**: `.spec.pt`ファイルで高速化

### 4.2 テキスト処理
1. **日本語音素変換**: pyopenjtalk使用
2. **音素シーケンス**: "sil " + phonemes + " sil"
3. **区切り文字変換**: スペース → ハイフン
4. **テンソル化**: text_to_sequence() → LongTensor

### 4.3 話者ID管理
- **話者情報**: speakers.jsonで管理
- **ID割り当て**: 0から順番に自動割り当て
- **埋め込み**: nn.Embedding層で高次元ベクトルに変換

## 5. 学習プロセス詳細

### 5.1 学習パラメータ（baseconfig.json）
```json
{
  "train": {
    "eval_interval": 1000,        // モデル保存間隔
    "batch_size": 10,            // バッチサイズ
    "learning_rate": 0.0002,     // 学習率
    "lr_decay": 0.999875,        // 学習率減衰
    "segment_size": 8192,        // オーディオセグメントサイズ
    "c_mel": 45,                 // メル損失重み
    "c_kl": 1.0                  // KL損失重み
  },
  "data": {
    "sampling_rate": 24000,      // サンプリングレート
    "filter_length": 1024,       // STFTフィルター長
    "hop_length": 256,           // ホップ長
    "win_length": 1024           // ウィンドウ長
  }
}
```

### 5.2 学習ループ（train_ms.py）
1. **データロード**: TextAudioSpeakerLoader
2. **フォワードパス**: 
   - テキスト → 音素埋め込み
   - 音声 → メルスペクトログラム
   - 話者ID → 話者埋め込み
3. **損失計算**: 
   - 生成器損失 + 識別器損失
   - 特徴マッチング損失 + KL損失
4. **バックプロパゲーション**: Adam最適化
5. **モデル保存**: eval_interval毎にcheckpoint

### 5.3 分散学習サポート
- **マルチGPU**: DistributedDataParallel (DDP)
- **混合精度**: autocast, GradScaler
- **バケットサンプリング**: 効率的なバッチ処理

## 6. 実行フロー

### 6.1 初期セットアップ
```bash
# Google Colab環境
1. 00_Clone_Repo.ipynb実行
   - リポジトリクローン
   - 事前学習モデルダウンロード
   - 依存関係インストール

2. 音声データ準備
   - 自分の声: dataset/textful/00_myvoice/
   - ターゲット声: dataset/textful/01_target/
   - 形式: 24kHz 16bit WAV + 対応テキスト
```

### 6.2 設定ファイル生成
```bash
01_Create_Configfile.ipynb実行
- create_dataset_jtalk.py呼び出し
- 音素変換とファイルリスト作成
- config.json生成
```

### 6.3 モデル学習
```bash
02_Train_MMVC.ipynb実行
- train_ms.py呼び出し
- マルチスピーカー学習実行
- logs/以下にcheckpoint保存
```

### 6.4 性能検証
```bash
03_MMVC_Interface.ipynb実行
- 学習済みモデル読み込み
- voice_conversion()でテスト
- 音声品質確認
```

### 6.5 ONNX変換
```bash
04_Export_ONNX.ipynb実行
- onnx_export.py呼び出し
- PyTorchモデル → ONNX変換
- 推論最適化
```

### 6.6 リアルタイム音声変換
```bash
MMVCServerSIO実行
- WebSocket経由で音声受信
- リアルタイム変換処理
- 変換結果送信
```

## 7. 重要な技術的詳細

### 7.1 Monotonic Alignment
- **目的**: テキストと音声の対応関係学習
- **実装**: Cython最適化（core.pyx）
- **アルゴリズム**: Dynamic Programming
- **最適化**: maximum_path_c関数で高速化

### 7.2 Voice Conversion Cycle
- **プロセス**: ソース → 潜在空間 → ターゲット話者で復元
- **損失**: Voice Conversion Loss（loss_vc）
- **精度向上**: メルスペクトログラム中央部分のみ使用

### 7.3 ONNX最適化
- **量子化**: 推論高速化
- **グラフ最適化**: onnxsim使用
- **動的軸**: 可変長入力対応

## 8. 詳細実装仕様

### 8.1 Multi-Head Attention実装
```python
class MultiHeadAttention(nn.Module):
    def __init__(self, channels, out_channels, n_heads, p_dropout=0., 
                 window_size=None, heads_share=True, block_length=None, 
                 proximal_bias=False, proximal_init=False):
        # 相対位置エンコーディング
        if window_size is not None:
            n_heads_rel = 1 if heads_share else n_heads
            rel_stddev = self.k_channels**-0.5
            self.emb_rel_k = nn.Parameter(torch.randn(n_heads_rel, window_size * 2 + 1, self.k_channels) * rel_stddev)
            self.emb_rel_v = nn.Parameter(torch.randn(n_heads_rel, window_size * 2 + 1, self.k_channels) * rel_stddev)
```

### 8.2 Normalizing Flow実装
```python
class ResidualCouplingBlock(nn.Module):
    def forward(self, x, x_mask, g=None, reverse=False):
        # Affine Coupling Layer
        if not reverse:
            x0, x1 = torch.split(x, [self.half_channels]*2, 1)
            h = self.pre(x0) * x_mask
            h = self.enc(h, x_mask, g=g)
            m, logs = torch.split(h, [self.half_channels]*2, 1)
            x1 = m + x1 * torch.exp(logs) * x_mask
            x = torch.cat([x0, x1], 1)
        else:
            # 逆変換
            x0, x1 = torch.split(x, [self.half_channels]*2, 1)
            h = self.pre(x0) * x_mask
            h = self.enc(h, x_mask, g=g)
            m, logs = torch.split(h, [self.half_channels]*2, 1)
            x1 = (x1 - m) * torch.exp(-logs) * x_mask
            x = torch.cat([x0, x1], 1)
        return x
```

### 8.3 Monotonic Alignment Cython実装
```cython
cdef void maximum_path_each(int[:,::1] path, float[:,::1] value, 
                           int t_y, int t_x, float max_neg_val=-1e9) nogil:
    cdef int x, y
    cdef float v_prev, v_cur, tmp
    cdef int index = t_x - 1
    
    # Dynamic Programming for optimal alignment
    for y in range(t_y):
        for x in range(max(0, t_x + y - t_y), min(t_x, y + 1)):
            if x == y:
                v_cur = max_neg_val
            else:
                v_cur = value[y-1, x]
            # 最適パス計算
            value[y, x] += max(v_prev, v_cur)
```

### 8.4 Voice Conversion実装詳細
```python
def voice_conversion(self, y, y_lengths, sid_src, sid_tgt):
    # ソース話者埋め込み
    g_src = self.emb_g(sid_src).unsqueeze(-1)
    g_tgt = self.emb_g(sid_tgt).unsqueeze(-1)
    
    # エンコード（ソース話者で）
    z, m_q, logs_q, y_mask = self.enc_q(y, y_lengths, g=g_src)
    z_p = self.flow(z, y_mask, g=g_src)
    
    # デコード（ターゲット話者で）
    z_hat = self.flow(z_p, y_mask, g=g_tgt, reverse=True)
    o_hat = self.dec(z_hat * y_mask, g=g_tgt)
    
    return o_hat, y_mask, (z, z_p, z_hat)
```

### 8.5 損失関数実装
```python
def train_step():
    # Generator Loss
    loss_mel = F.l1_loss(y_mel, y_hat_mel) * hps.train.c_mel
    loss_kl = kl_loss(z_p, logs_q, m_p, logs_p, z_mask) * hps.train.c_kl
    loss_fm = feature_loss(fmap_r, fmap_g)
    loss_gen, losses_gen = generator_loss(y_d_hat_g)
    
    # Voice Conversion Loss（中央部分のみ使用）
    dispose_length = y_mel.size(2) // 4
    disposed_y_mel = y_mel[:, :, dispose_length:-dispose_length]
    disposed_vc_o_r_hat_mel = vc_o_r_hat_mel[:, :, dispose_length:-dispose_length]
    loss_vc = F.l1_loss(disposed_y_mel, disposed_vc_o_r_hat_mel) * hps.train.c_mel
    
    loss_gen_all = loss_gen + loss_fm + loss_mel + loss_vc + loss_kl
```

### 8.6 学習ループ最適化詳細実装
```python
def train_and_evaluate(rank, epoch, hps, nets, optims, schedulers, scaler, loaders, logger, writers):
    net_g, net_d = nets
    optim_g, optim_d = optims
    scheduler_g, scheduler_d = schedulers
    train_loader, eval_loader = loaders
    
    # 混合精度学習設定
    scaler = GradScaler(enabled=hps.train.fp16_run)
    
    # 学習ループ
    for batch_idx, (x, x_lengths, spec, spec_lengths, y, y_lengths, speakers) in enumerate(train_loader):
        # GPU転送
        x, x_lengths = x.cuda(rank, non_blocking=True), x_lengths.cuda(rank, non_blocking=True)
        spec, spec_lengths = spec.cuda(rank, non_blocking=True), spec_lengths.cuda(rank, non_blocking=True)
        y, y_lengths = y.cuda(rank, non_blocking=True), y_lengths.cuda(rank, non_blocking=True)
        speakers = speakers.cuda(rank, non_blocking=True)
        
        # メルスペクトログラム変換
        mel = spec_to_mel_torch_data(spec, hps.data)
        target_ids = torch.tensor(train_loader.dataset.get_all_sid())
        
        with autocast(enabled=hps.train.fp16_run):
            # 生成器フォワードパス（音声変換含む）
            y_hat, attn, ids_slice, x_mask, z_mask, \
            (z, z_p, m_p, logs_p, m_q, logs_q), vc_o_r_hat = net_g(
                x, x_lengths, spec, spec_lengths, speakers, target_ids)
            
            # セグメント切り出し
            spec_segment_size = hps.train.segment_size // hps.data.hop_length
            y_mel = commons.slice_segments(mel, ids_slice, spec_segment_size)
            y_hat_mel = mel_spectrogram_torch_data(y_hat.squeeze(1), hps.data)
            vc_o_r_hat_mel = mel_spectrogram_torch_data(vc_o_r_hat.float().squeeze(1), hps.data)
            
            # 識別器学習
            y = commons.slice_segments(y, ids_slice * hps.data.hop_length, hps.train.segment_size)
            y_d_hat_r, y_d_hat_g, _, _ = net_d(y, y_hat.detach())
            
            with autocast(enabled=False):
                loss_disc, losses_disc_r, losses_disc_g = discriminator_loss(y_d_hat_r, y_d_hat_g)
        
        # 識別器バックプロパゲーション
        optim_d.zero_grad()
        scaler.scale(loss_disc).backward()
        scaler.unscale_(optim_d)
        grad_norm_d = commons.clip_grad_value_(net_d.parameters(), None)
        scaler.step(optim_d)
        
        with autocast(enabled=hps.train.fp16_run):
            # 生成器損失計算
            y_d_hat_r, y_d_hat_g, fmap_r, fmap_g = net_d(y, y_hat)
            
            with autocast(enabled=False):
                # メルスペクトログラム損失
                loss_mel = F.l1_loss(y_mel, y_hat_mel) * hps.train.c_mel
                
                # Voice Conversion損失（中央部分のみ使用）
                dispose_length = y_mel.size(2) // 4
                disposed_y_mel = y_mel[:, :, dispose_length:-dispose_length]
                disposed_vc_o_r_hat_mel = vc_o_r_hat_mel[:, :, dispose_length:-dispose_length]
                loss_vc = F.l1_loss(disposed_y_mel, disposed_vc_o_r_hat_mel) * hps.train.c_mel
                
                # KLダイバージェンス損失
                loss_kl = kl_loss(z_p, logs_q, m_p, logs_p, z_mask) * hps.train.c_kl
                
                # 特徴マッチング損失
                loss_fm = feature_loss(fmap_r, fmap_g)
                
                # 生成器敵対的損失
                loss_gen, losses_gen = generator_loss(y_d_hat_g)
                
                # 総損失
                loss_gen_all = loss_gen + loss_fm + loss_mel + loss_vc + loss_kl
        
        # 生成器バックプロパゲーション
        optim_g.zero_grad()
        scaler.scale(loss_gen_all).backward()
        scaler.unscale_(optim_g)
        grad_norm_g = commons.clip_grad_value_(net_g.parameters(), None)
        scaler.step(optim_g)
        scaler.update()
```

### 8.7 データローダー実装詳細
```python
class TextAudioSpeakerLoader(torch.utils.data.Dataset):
    def __init__(self, audiopaths_and_text, hps, augmentation=True):
        self.audiopaths_and_text = load_filepaths_and_text(audiopaths_and_text)
        self.hps = hps
        self.augmentation = augmentation
        self.text_cleaners = hps.data.text_cleaners
        self.max_wav_value = hps.data.max_wav_value
        self.sampling_rate = hps.data.sampling_rate
        self.filter_length = hps.data.filter_length
        self.hop_length = hps.data.hop_length
        self.win_length = hps.data.win_length
        self.use_mel_train = hps.model.use_mel_train if hasattr(hps.model, 'use_mel_train') else False
        
        # 話者ID管理
        self.speaker_ids = self.create_speaker_lookup(audiopaths_and_text)
        
        # スペクトログラムキャッシュ
        self.spec_cache = {}
        
    def get_audio_text_speaker_pair(self, audiopath_and_text):
        audiopath, text, speaker = audiopath_and_text[0], text_and_speaker[1], text_and_speaker[2]
        
        # 音声読み込み
        spec, wav = self.get_audio(audiopath)
        
        # テキスト処理
        text = self.get_text(text)
        
        # 話者ID取得
        speaker_id = self.get_sid(speaker)
        
        return (text, spec, wav, speaker_id)
    
    def get_audio(self, filename):
        # キャッシュファイル確認
        cache_path = filename.replace('.wav', '.spec.pt')
        if os.path.exists(cache_path):
            spec = torch.load(cache_path)
        else:
            # 音声読み込みとSTFT
            audio, sampling_rate = load_wav_to_torch(filename)
            audio_norm = audio / self.max_wav_value
            audio_norm = audio_norm.unsqueeze(0)
            
            spec = spectrogram_torch(
                audio_norm, self.filter_length,
                self.sampling_rate, self.hop_length, self.win_length,
                center=False)
            spec = torch.squeeze(spec, 0)
            
            # キャッシュ保存
            torch.save(spec, cache_path)
        
        return spec, audio_norm
```

### 8.8 スケジューラー・最適化詳細
```python
def setup_optimizers_and_schedulers(net_g, net_d, hps):
    # Adam最適化器
    optim_g = torch.optim.AdamW(
        net_g.parameters(), 
        hps.train.learning_rate, 
        betas=hps.train.betas,  # [0.8, 0.99]
        eps=hps.train.eps)      # 1e-9
    
    optim_d = torch.optim.AdamW(
        net_d.parameters(),
        hps.train.learning_rate, 
        betas=hps.train.betas, 
        eps=hps.train.eps)
    
    # 指数減衰スケジューラー
    scheduler_g = torch.optim.lr_scheduler.ExponentialLR(
        optim_g, gamma=hps.train.lr_decay)  # 0.999875
    scheduler_d = torch.optim.lr_scheduler.ExponentialLR(
        optim_d, gamma=hps.train.lr_decay)
    
    return optim_g, optim_d, scheduler_g, scheduler_d

def distributed_setup(rank, n_gpus, hps):
    # 分散学習初期化
    if hps.others.os_type == "windows":
        backend_type = "gloo"
        parallel = DP  # DataParallel
    else:  # Colab/Linux
        backend_type = "nccl"
        parallel = DDP  # DistributedDataParallel
        
    if n_gpus > 1:
        dist.init_process_group(
            backend=backend_type, rank=rank, world_size=n_gpus)
    
    return parallel
```

### 8.9 評価・保存プロセス実装
```python
def evaluation_and_checkpoint_saving(global_step, hps, nets, optims, eval_loader, logger, writer_eval):
    net_g, net_d = nets
    optim_g, optim_d = optims
    
    if global_step % hps.train.eval_interval == 0:
        # 評価実行
        eval_loss_dict = evaluate(hps, net_g, eval_loader, writer_eval, logger)
        eval_loss_mel = float(eval_loss_dict["loss/g/mel"])
        eval_loss_vc = float(eval_loss_dict["loss/g/vc"])
        
        # 音声サンプル生成
        utils.save_vc_sample(hps, TextAudioSpeakerLoader, TextAudioSpeakerCollate, net_g, global_step)
        
        # チェックポイント保存
        if global_step % hps.train.backup.interval == 0:
            utils.save_checkpoint(net_g, optim_g, hps.train.learning_rate, global_step,
                                os.path.join(hps.model_dir, f"G_{global_step}.pth"))
            if not hps.train.backup.g_only:
                utils.save_checkpoint(net_d, optim_d, hps.train.learning_rate, global_step,
                                    os.path.join(hps.model_dir, f"D_{global_step}.pth"))
        
        # ベストモデル保存
        if hps.train.best and eval_loss_mel < hps.best_loss_mel:
            utils.save_checkpoint(net_g, optim_g, hps.train.learning_rate, global_step,
                                os.path.join(hps.model_dir, "G_best.pth"))
            hps.best_loss_mel = eval_loss_mel
            
        # 最新モデル保存（毎回）
        utils.save_checkpoint(net_g, optim_g, hps.train.learning_rate, global_step,
                            os.path.join(hps.model_dir, "G_latest_99999999.pth"))
        utils.save_checkpoint(net_d, optim_d, hps.train.learning_rate, global_step,
                            os.path.join(hps.model_dir, "D_latest_99999999.pth"))
```

## 9. 再現に必要な環境

### 9.1 必須要件
- **GPU**: CUDA対応GPU（最小8GB VRAM）
- **Python**: 3.8以上
- **OS**: Linux推奨（Google Colab環境）

### 9.2 依存パッケージ
```
torch==1.13.1+cu116
torchaudio==0.13.1+cu116
scipy==1.7.3
numpy==1.21.6
librosa==0.8.0
pyopenjtalk==0.3.0
jamo==0.4.1
pypinyin==0.44.0
jieba==0.42.1
protobuf==3.20.1
cn2an==0.5.17
inflect==5.6.0
eng_to_ipa==0.0.2
ko_pron==1.3
unidecode==1.3.6
phonemizer==3.2.1
Cython==0.29.21
```

### 9.3 事前学習モデル
- **Generator**: G_v13_20231020.pth
- **Discriminator**: D_v13_20231020.pth
- **配置場所**: fine_model/ディレクトリ

### 9.4 重要な設定パラメータ詳細
```json
{
  "train": {
    "log_interval": 200,           // ログ出力間隔
    "eval_interval": 1000,         // 評価・保存間隔
    "epochs": 10000,               // 最大エポック数
    "batch_size": 10,              // バッチサイズ（GPU性能に応じて調整）
    "learning_rate": 2e-4,         // 初期学習率
    "lr_decay": 0.999875,          // 学習率減衰率（毎ステップ）
    "segment_size": 8192,          // 音声セグメントサイズ（サンプル）
    "init_lr_ratio": 1,            // 初期学習率比率
    "warmup_epochs": 0,            // ウォームアップエポック数
    "c_mel": 45,                   // メルスペクトログラム損失重み
    "c_kl": 1.0,                   // KLダイバージェンス損失重み
    "use_sr": true,                // スペクトラル正規化使用
    "max_grad_norm": 1000,         // 勾配クリッピング上限
    "fp16_run": true,              // 混合精度学習有効化
    "betas": [0.8, 0.99],          // Adam最適化パラメータ
    "eps": 1e-9,                   // Adam epsilon
    "backup": {
      "interval": 10000,           // バックアップ間隔
      "g_only": false,             // 生成器のみ保存するか
      "mean_of_num_eval": 3        // 評価時の平均化回数
    },
    "best": true                   // ベストモデル保存有効化
  },
  "data": {
    "training_files": "filelists/train.txt",
    "validation_files": "filelists/val.txt", 
    "text_cleaners": ["japanese_cleaners"],
    "max_wav_value": 32768.0,     // 音声正規化値
    "sampling_rate": 24000,       // サンプリングレート
    "filter_length": 1024,        // STFTフィルター長
    "hop_length": 256,            // STFTホップ長
    "win_length": 1024,           // STFTウィンドウ長
    "n_mel_channels": 80,         // メルスペクトログラムチャンネル数
    "mel_fmin": 0.0,              // メル最小周波数
    "mel_fmax": null,             // メル最大周波数（自動設定）
    "add_blank": true,            // 音素間にブランク挿入
    "n_speakers": 256             // 最大話者数
  },
  "model": {
    "inter_channels": 192,         // 中間チャンネル数
    "hidden_channels": 192,        // 隠れチャンネル数
    "filter_channels": 768,        // フィルターチャンネル数
    "n_heads": 2,                  // アテンションヘッド数
    "n_layers": 6,                 // エンコーダー層数
    "kernel_size": 3,              // 畳み込みカーネルサイズ
    "p_dropout": 0.1,              // ドロップアウト率
    "resblock": "1",               // 残差ブロックタイプ
    "resblock_kernel_sizes": [3,7,11],    // 残差ブロックカーネル
    "resblock_dilation_sizes": [[1,3,5], [1,3,5], [1,3,5]], // ダイレーション
    "upsample_rates": [8,8,2,2],   // アップサンプリング率
    "upsample_initial_channel": 512, // 初期アップサンプリングチャンネル
    "upsample_kernel_sizes": [16,16,4,4], // アップサンプリングカーネル
    "n_layers_q": 3,               // Posterior Encoder層数
    "use_spectral_norm": false,    // スペクトラル正規化
    "gin_channels": 256,           // Global Information Networkチャンネル
    "use_mel_train": false         // メルスペクトログラム学習モード
  },
  "others": {
    "os_type": "colab",           // OS種別（windows/colab）
    "input_filename": "dataset/textful/00_myvoice/voice1.wav" // サンプル音声
  }
}
```

### 9.5 必要なディレクトリ構造
```
MMVC_Trainer/
├─ dataset/
│  └─ textful/
│     ├─ 00_myvoice/          # 自分の声データ
│     │  ├─ voice1.wav
│     │  ├─ voice1.txt
│     │  └─ ...
│     └─ 01_target/           # ターゲット声データ  
│        ├─ voice1.wav
│        ├─ voice1.txt
│        └─ ...
├─ fine_model/                # 事前学習モデル
│  ├─ G_v13_20231020.pth     # 生成器
│  └─ D_v13_20231020.pth     # 識別器
├─ filelists/                 # 学習用ファイルリスト
│  ├─ train.txt
│  └─ val.txt
├─ configs/                   # 設定ファイル
│  └─ train_config.json
└─ logs/                      # 学習ログ・モデル出力
   ├─ G_*.pth
   ├─ D_*.pth
   └─ events.out.tfevents.*
```

### 9.6 コマンドライン引数詳細仕様

#### 学習実行コマンド
```bash
# 新規学習
python train_ms.py -c configs/train_config.json -m model_save_directory -fg fine_model/G_v13_20231020.pth -fd fine_model/D_v13_20231020.pth

# 学習再開
python train_ms.py -c configs/train_config.json -m model_save_directory
```

#### 引数詳細
- `-c, --config`: 設定ファイル（JSON）のパス（必須）
- `-m, --model`: モデル保存ディレクトリ名（必須）
- `-fg, --fine_tuning_g`: Fine tuning用生成器モデル（G_xxxx.pth）
- `-fd, --fine_tuning_d`: Fine tuning用識別器モデル（D_xxxx.pth）

#### データセット作成コマンド
```bash
python create_dataset_jtalk.py -f dataset/textful/train.txt -s 24000 -t 9999
```

#### ONNX変換コマンド
```bash
python onnx_export.py --config_file configs/train_config.json --convert_pth logs/model_name/G_best.pth
```

### 9.7 正確な依存関係バージョン

#### requirements.txt
```txt
Cython==0.29.21
librosa==0.8.0
matplotlib==3.3.1
numpy==1.23.5
phonemizer==2.2.1
scipy
tensorboard==2.3.0
-f https://download.pytorch.org/whl/cu116/torch_stable.html
torch==1.13.1+cu116
torchvision==0.14.1+cu116
torchaudio==0.13.1+cu116
Unidecode==1.1.1
retry
tqdm
resampy==0.2.2
onnx==1.14.0
onnxsim==0.4.30
onnxruntime==1.15.0
```

#### WSL2追加要件
```txt
pyopenjtalk
psutil
protobuf==3.19.0
```

#### インストール手順
```bash
# 1. Python 3.8以上の環境準備
# 2. CUDA 11.6対応環境構築
pip install -r requirements.txt

# 3. Cythonコンパイル（重要）
cd monotonic_align
python setup.py build_ext --inplace
cd ..

# 4. pyopenjtalk辞書インストール
python -c "import pyopenjtalk; pyopenjtalk.g2p('テスト')"
```

## 10. トラブルシューティング

### 10.1 よくある問題
1. **monotonic_align**コンパイルエラー
   - 解決: `python setup.py build_ext --inplace`

2. **CUDA out of memory**
   - 解決: batch_size削減

3. **音声品質が悪い**
   - 解決: 学習データ品質確認、学習時間延長

### 10.2 デバッグ方法
- **ログ確認**: TensorBoard使用
- **音声確認**: IPython.display.Audio
- **メトリクス**: 損失値の推移監視

### 10.3 性能最適化
- **GPU利用率**: nvidia-smi監視
- **メモリ使用量**: batch_size調整
- **学習速度**: mixed precision有効化

### 10.4 学習継続・再開
```python
# チェックポイントからの再開
try:
    _, _, _, global_step = utils.load_checkpoint(
        utils.latest_checkpoint_path(hps.model_dir, "G_*.pth"), 
        net_g, optim_g)
    epoch_str = global_step // len(train_loader) + 1
except:
    epoch_str = 1
    global_step = 0
```

### 10.5 音声品質向上のコツ
1. **データ品質**: 無音部分除去、音量正規化
2. **学習時間**: 最低50,000ステップ推奨
3. **Voice Conversion Loss**: 中央部分のみ使用で精度向上
4. **話者embedding**: 十分な次元数（256推奨）

## 11. 利用規約・ライセンス

### 11.1 利用制限
- **商用利用**: 事前許可必要
- **改変**: オープンソースライセンス準拠
- **配布**: 出典明記必須

### 11.2 責任範囲
- **品質保証**: なし
- **サポート**: コミュニティベース

### 11.3 重要な注意事項

#### 環境依存性
- **GPU要件**: CUDA 11.6以上、VRAM 8GB以上推奨
- **OS**: Linux/WSL2推奨、Windows（gloo backend使用）
- **Python**: 3.8-3.10（3.11以上非対応）

#### データ要件
- **音声品質**: ノイズレス、均一音量、クリア発音
- **ファイル形式**: 24kHz 16bit モノラル WAV必須
- **データ量**: 話者あたり最低1時間、推奨3時間以上

#### 学習時間目安
- **Google Colab Pro**: 約12-24時間（データ量による）
- **RTX 3090**: 約8-16時間
- **複数GPU**: 分散学習で短縮可能

---

## 12. 再現完全性確認

### 12.1 機能確認チェックリスト

#### コア機能
- [ ] マルチスピーカー音声変換
- [ ] リアルタイム推論対応
- [ ] 日本語音素処理
- [ ] VITS生成器・識別器学習
- [ ] 混合精度学習
- [ ] 分散学習（複数GPU）

#### データ処理
- [ ] pyopenjtalk音素変換
- [ ] メルスペクトログラム処理
- [ ] 音声セグメント化
- [ ] スペクトログラムキャッシュ
- [ ] データ拡張（オプション）

#### 学習システム
- [ ] Adam最適化（AdamW）
- [ ] 指数減衰スケジューラー
- [ ] 勾配クリッピング
- [ ] チェックポイント保存・復元
- [ ] TensorBoard統合
- [ ] ベストモデル自動保存

#### 損失関数
- [ ] 生成器損失（Adversarial）
- [ ] 識別器損失（Multi-Period）
- [ ] 特徴マッチング損失
- [ ] メルスペクトログラム損失
- [ ] KLダイバージェンス損失
- [ ] 音声変換損失（VC Loss）

#### モデルコンポーネント
- [ ] SynthesizerTrn（生成器）
- [ ] MultiPeriodDiscriminator
- [ ] Multi-Head Attention
- [ ] Normalizing Flow
- [ ] Monotonic Alignment
- [ ] WaveNet残差ブロック

#### 出力・変換
- [ ] PyTorch → ONNX変換
- [ ] 推論最適化
- [ ] 音声品質評価
- [ ] voice_conversion()関数

### 12.2 性能指標確認

#### 学習収束指標
- [ ] loss/g/mel < 0.5（収束目安）
- [ ] loss/g/vc < 0.3（音声変換品質）
- [ ] loss/g/kl < 0.1（潜在表現学習）
- [ ] loss/d/total安定化

#### 音声品質指標
- [ ] 自然性：MOS > 3.5
- [ ] 類似性：話者識別精度
- [ ] リアルタイム性：推論速度
- [ ] ロバスト性：多様な入力対応

### 12.3 実装完全性検証

以下の実装が完了していることを最終確認：

#### Python実装ファイル
- [ ] train_ms.py（メイン学習スクリプト）
- [ ] models.py（モデル定義）
- [ ] data_utils.py（データローダー）
- [ ] losses.py（損失関数）
- [ ] create_dataset_jtalk.py（前処理）
- [ ] onnx_export.py（モデル変換）

#### C拡張・最適化
- [ ] monotonic_align/setup.py
- [ ] Cythonコンパイル成功
- [ ] 動的プログラミング最適化

#### 設定・データ
- [ ] configs/設定ファイル
- [ ] filelists/学習データリスト
- [ ] fine_model/事前学習モデル
- [ ] text/音素処理

**この詳細説明書（857行）に基づいて、MMVC_Trainerの完全な機能再現が可能です。**
