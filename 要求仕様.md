・リファレンスに記載されているファイル構造を無視し、より適切なファイル構造を検討すること。例えばsrcディレクトリ内にソースコードを保存するなど。
・リファレンスに記載されているファイル名を無視し、より適切なファイル名を検討すること。
・リファレンスの仕様ではJupyterノートブックで操作を行っているが、PythonスクリプトでCLIベースで操作を行うようにすること。Colabで実行する場合は、Pythonスクリプトを起動するシンプルなJupyterノートブックを用いるものとする。なお、Jupyterノートブックでは以下のようにノートブック上で入力を要求できる。
```
num_input = input("数値を入力してください: ")
print("入力された数値:", num_input)
```
```
数値を入力してください: <入力欄が表示される>
入力された数値: 1000000
```